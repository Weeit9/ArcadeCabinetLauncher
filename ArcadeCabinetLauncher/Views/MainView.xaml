<Page x:Class="ArcadeCabinetLauncher.ViewModels.MainView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ArcadeCabinetLauncher.ViewModels"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="MainView">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition/>
            
        </Grid.RowDefinitions>

        <Menu Grid.Row="0">
            <MenuItem Header="File" FontSize="16">
                <MenuItem Header="Add Game" Command="{Binding AddGameCommand}"/>
                <MenuItem Header="Start Game" Command="{Binding StartGameCommand}"/>

            </MenuItem>
        </Menu>

        <ListView Name="GamesLV" SelectionMode="Single" Grid.Row="1" ItemsSource="{Binding Games}" SelectedItem="{Binding SelectedGame, Mode=TwoWay}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid ShowGridLines="True">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="50"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="100"/>
                            <ColumnDefinition Width="500"/>
                        </Grid.ColumnDefinitions>

                        <Button Grid.Column="0"
                                Width="48"
                                Height="48"
                                Padding="0"
                                Background="Transparent"
                                Command="{Binding StartGameCommand, RelativeSource={RelativeSource AncestorType=ListView}}">
                            <Image Source="{Binding ThumbnailPath}" Stretch="Uniform" IsHitTestVisible="False"></Image>
                        </Button>

                        <TextBlock Grid.Column="1" Text="{Binding Name}" Padding="10"/>

                        <TextBlock Grid.Column="2" Text="{Binding GameMaker}" Padding="10"/>

                        <TextBlock Grid.Column="3" Text="{Binding Description}" Padding="10"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
